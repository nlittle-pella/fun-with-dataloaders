type Author {
    id: ID!
}

input InputAuthor {
    id: String
}

input CreateAuthorInput {
    author: InputAuthor
}

type Mutation {
    createAuthor(input: CreateAuthorInput!): Author!
}

type Query {
    getAuthorById(id: String!): Author
}
